import { Component } from '@angular/core';

/**
 * Contact links component.
 *
 * Renders the persistent social/professional link icons
 * (e.g. LinkedIn, GitHub, email) displayed in the header.
 * Static presentational component with no logic.
 */
@Component({
  selector: 'app-contact-links',
  imports: [],
  templateUrl: './contact-links.html',
  styleUrl: './contact-links.scss',
})
export class ContactLinks {}
